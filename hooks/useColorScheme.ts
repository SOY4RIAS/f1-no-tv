// biome-ignore lint/performance/noBarrelFile: this is needed cause it can be handle by multiple platforms and there are a web version see: `./useColorScheme.web.ts`
export { useColorScheme } from 'react-native';
